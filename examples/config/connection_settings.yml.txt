---
# file: connection_settings.yml

# Copy the file to connection_settings.yml
# Change the following parameters to bind to ActiveDirectory

:host: __FQDN__
:base: __BASEDN__
:auth:
  :username: __SERVICE_ACCT_RDN__ 
  :password: '__PASSWORD__'
  :method: :simple

# Options  for :port and :encryption have been already set
# to bind to ActiveDirectory through ldaps://

...
